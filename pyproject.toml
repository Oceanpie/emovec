[project]
name = "emovec"
version = "0.1.0"
description = "Zero-shot Chinese text to 8D emotion intensity vector"
requires-python = ">=3.12"
dependencies = [
    "numpy>=1.24",
    "scikit-learn>=1.3",
    "matplotlib>=3.7",
    "umap-learn>=0.5",
    "openai>=1.0",
    "tqdm>=4.65",
]
